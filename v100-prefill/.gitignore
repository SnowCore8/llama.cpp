# 本目录是 V100 预填充调优资料包（见 fa-prefill-experiments.md）。
# 证据日志按设计入库：覆盖仓库根 .gitignore 的 *.log 忽略。
!fa-logs/*.log

# 内核库快照（构建产物，单文件 167 MB）不入库，仅本地保留。
fa-libs/

# 原始 nsys 轨迹（二进制剖析产物）：内含被剖析进程的环境变量，曾把 API key 一并捕获，
# 因此不入库（本地副本已删除）；可读结论在 fa-logs/*-kern.csv 与 *-stats.txt。
fa-logs/*.nsys-rep
